#include <stdlib.h>
#include <stdio.h>
/**
 *times_table-Entry point for the print_sign function
 *
 *Description:using a function to print out nested loop
 *
 *Return:Always 0 (Success)
 */
void times_table(void);
{

	int n;
	int m;
	int linebr = 10;
	for (n = 0 ; n <= 9 ; n++)
	{
		for (m = 0 ; m <= 9 ; m++)
		{
			int ans = n + ans;
		}
		putchar(linebr);

	}

}
